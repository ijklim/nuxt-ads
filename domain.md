# Domain Layer Setup Script

Due to environment limitations (bash not available), please run this command manually to create the domain directory structure:

```bash
mkdir -p domain/shared domain/ads tests/unit/domain
```

Then the domain layer files will be created.
